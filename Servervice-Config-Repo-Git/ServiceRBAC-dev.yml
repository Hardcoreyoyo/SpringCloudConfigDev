server:
  port: 8401

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/eureka

feign:
  okhttp:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: refresh,health

github:
  test: 2022/0329/0057 for github test
