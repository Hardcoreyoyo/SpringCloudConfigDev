server:
  port: 8401

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/eureka

feign:
  okhttp:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: refresh,health

github:
  test: 202203282354-for-github-test
